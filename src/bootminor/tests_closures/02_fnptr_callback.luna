fn add(@a: int, @b: int) -> int
    return @a + @b

fn sub(@a: int, @b: int) -> int
    return @a - @b

fn apply(@op: int, @a: int, @b: int) -> int
    @f = @op
    return @f(@a, @b)

fn main() -> int
    shine_int(apply(add, 30, 12))
    shine_int(apply(sub, 30, 12))
    return 0
