fn sq(@x: int) -> int
    return @x * @x

fn inc(@x: int) -> int
    return @x + 1

fn dec(@x: int) -> int
    return @x - 1

fn main() -> int
    @arr = [sq, inc, dec]
    @i = 0
    while @i < 3
        @f = @arr[@i]
        shine_int(@f(10))
        @i = @i + 1
    return 0
