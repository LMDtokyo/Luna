fn identity<T>(@x: T) -> T
    return @x

fn apply<T>(@f_arg: T, @extra: int) -> int
    return @f_arg + @extra

fn main() -> int
    shine_int(identity<int>(42))
    shine_int(identity<u32>(100))
    shine_int(apply<int>(30, 12))
    return 0
